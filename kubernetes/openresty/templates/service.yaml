apiVersion: v1
kind: Service

metadata:
  name: library-gateway
  labels:
    app: library-gateway

spec:
  type: LoadBalancer

  selector:
    app: library-gateway

  ports:
    - name: http
      port: 80
      targetPort: http