apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalogue
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: catalogue
  endpoints:
    - port: http
      path: /api/catalogue/actuator/prometheus
      interval: 15s