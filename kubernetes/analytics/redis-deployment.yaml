apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-redis
  template:
    metadata:
      labels:
        app: analytics-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379