apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: library-ingress

spec:
  ingressClassName: nginx

  rules:

    - host: library.damou.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: library-frontend
                port:
                  number: 80

    - host: api.damou.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: library-gateway
                port:
                  number: 80

    - host: jaeger.damou.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jaeger
                port:
                  number: 16686

    - host: grafana.damou.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monitoring-grafana
                port:
                  number: 80

    - host: prometheus.damou.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monitoring-kube-prometheus-prometheus
                port:
                  number: 9090